---
/**
 * Keystatic Route Handler
 *
 * This file is not directly used because keystatic() is configured as an integration
 * in astro.config.mjs, which automatically handles the /keystatic routes.
 *
 * The navigation bar is injected via:
 * - /public/keystatic-nav.js (the script that creates the nav bar)
 * - src/middleware.ts (injects the script tag into Keystatic HTML pages)
 */
---
